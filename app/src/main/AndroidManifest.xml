<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rpl.ezy.olread">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.user.ProfileActivity"></activity>
        <activity android:name=".view.user.HistoryActivity" />
        <activity android:name=".view.user.ArchiveActivity" />
        <activity android:name=".view.user.FavoriteActivity" />
        <activity android:name=".view.user.UserActivity" />
        <activity android:name=".view.RecipeDetailActivity" />
        <activity android:name=".view.admin.AdminActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".view.auth.Authentification"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>