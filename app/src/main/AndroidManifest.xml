<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rpl.ezy.olread">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.user.EditProfile" />
        <activity android:name=".view.user.AddRecipesActivity" />
        <activity android:name=".view.user.ArchiveActivity" />
        <activity android:name=".view.user.SearchActivity" />
        <activity android:name=".view.user.CategoryActivity" />
        <activity android:name=".view.user.ProfileActivity" />
        <activity android:name=".view.user.HistoryActivity" />
        <activity android:name=".view.user.FavoriteActivity" />
        <activity android:name=".view.user.UserActivity" />
        <activity android:name=".view.RecipeDetailActivity" />
        <activity android:name=".view.admin.AdminActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".view.auth.Authentification">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>